import React from "react";
import PredictLayout from "./PredictLayout";

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <PredictLayout>{children}</PredictLayout>;
};
export default Layout;
